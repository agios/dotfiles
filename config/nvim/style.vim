" Fonts and colors
let g:airline_powerline_fonts = 1

"colorscheme solarized
"colorscheme base16-railscasts
if !exists("g:colors_name") || g:colors_name != 'alex'
  colorscheme alex
endif

" Vim Airline
"let g:airline_theme = 'base16'

